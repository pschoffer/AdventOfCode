{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "name": "Rust",
            "request": "launch",
            "program": "advent",
            "cwd": "${relativeFileDirname}/.."
        }
    ]
}